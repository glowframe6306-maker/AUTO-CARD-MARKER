import { useEffect } from "react";
import type { AppProps } from "next/app";
import "../styles/globals.css";
import AppShell from "../components/AppShell";

export default function App({ Component, pageProps, router }: AppProps) {
  useEffect(() => {`r`n    if (typeof window !== "undefined") {`r`n      applyGlobalLanguage(getStoredLanguage());`r`n      const savedTheme = (localStorage.getItem("app-theme") as "system" | "light" | "dark") || "system";`r`n      applyGlobalAppearance(savedTheme);`r`n    }`r`n    if (typeof window !== "undefined") {`r`n      applyGlobalLanguage(getStoredLanguage());`r`n      const savedTheme = (localStorage.getItem("app-theme") as "system" | "light" | "dark") || "system";`r`n      applyGlobalAppearance(savedTheme);`r`n    }
    const applySettings = () => {
      const theme = localStorage.getItem("app-theme") || "system";
      const language = localStorage.getItem("app-language") || "English";

      const root = document.documentElement;

      if (theme === "dark") {
        root.setAttribute("data-theme", "dark");
        root.classList.add("dark");
      } else if (theme === "light") {
        root.setAttribute("data-theme", "light");
        root.classList.remove("dark");
      } else {
        const prefersDark = window.matchMedia(
          "(prefers-color-scheme: dark)"
        ).matches;

        root.setAttribute(
          "data-theme",
          prefersDark ? "dark" : "light"
        );

        root.classList.toggle("dark", prefersDark);
      }

      root.setAttribute("lang", language.toLowerCase());
    };

    applySettings();

    window.addEventListener("settings-updated", applySettings);

    return () => {
      window.removeEventListener("settings-updated", applySettings);
    };
  }, []);

  if (router.pathname === "/") {
    return <Component {...pageProps} />;
  }

  return (
    <AppShell>
      <Component {...pageProps} />
    </AppShell>
  );
}



